const user = function(date){
    return `${date} is logged in.`
}

const id = function(id){
    return `${id}`
}

const email = function(email){
    return `${email}`
}

module.exports = {
    user, 
    id, 
    email
};